# Planes futuros

- Explorar un modo "watch folder": convertir automáticamente cualquier `.svg`
  nuevo que aparezca en una carpeta vigilada.
- Integrar generación de `favicon.ico` + manifest web (PWA) en un solo flujo.
- Evaluar soporte multiplataforma (macOS/Linux) más allá del instalador Windows.
